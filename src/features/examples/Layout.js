import React, { Component } from 'react';

export default class Layout extends Component {
  static propTypes = {

  };

  render() {
    return (
      <div className="examples-layout">
        Component content: examples/Layout
      </div>
    );
  }
}
